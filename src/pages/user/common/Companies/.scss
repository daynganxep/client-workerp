.home-container {
  padding: 24px;

  .header-box {
    margin-bottom: 24px;
  }

  .company-card {
    height: 100%;
    transition: transform 0.2s ease-in-out;

    &:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    }
  }
}
