.auth-layout {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;

  &__background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.5;
    z-index: 0;
  }

  &__container {
    width: 100%;
    max-width: 450px;
    z-index: 1;
    padding: 24px;
    backdrop-filter: blur(2px);
    box-shadow: 0 0 20px 20px inset rgba(0, 98, 255, 0.05);
    border-radius: 16px;
  }

  &__header {
    display: flex;
    flex-direction: row;
    align-items: end;
    justify-content: center;
    gap: 12px;
    text-decoration: none;

    img {
      height: 60px;
    }
  }
}
