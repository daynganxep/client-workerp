.main-layout {
    display: none;
    display: grid;
    grid-template-rows: auto 1fr;
    gap: 16px;
    padding: 8px;

    &__header {
        position: fixed;
        border-radius: 8px;
    }

    &__content {
        border-radius: 8px;
        margin: 0 8px 8px 8px;
        overflow-y: auto;
        display: flex;
        flex-direction: column;
    }

    &__container {
        flex-grow: 1;
        padding: 24px;
    }
}
